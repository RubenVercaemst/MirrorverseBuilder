import { FilterSelectedPipe } from './filter-selected.pipe';

describe('FilterSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
